"""ReactiveBuild replication -- analysis & plotting (Phase 7).

Reads the C++ simulator's CSV output and computes scaling-law fits + figures. Produces no
physics of its own (the simulation lives in reactivebuild/cpp). See tower.py for section 4.1.
"""
